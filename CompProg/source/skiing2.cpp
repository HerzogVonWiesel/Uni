
#include <bits/stdc++.h>

#define rep(a, b)   for(int a = 0; a < (b); ++a)
#define all(a)      (a).begin(),(a).end()
//#define endl        '\n' //comment this out for interactive problems

using namespace std;
using Graph = vector<vector<int>>;
using ll = long long;

ll ceil_div(const ll a, const ll b){
   return (a + b - 1) / b;
}

ll find_summit(ll l, ll m, ll r){
    bool higher;
    ll lm;
    ll lr;
    if (l == m && m == r) return m;

    cout << "? " << l << " " << m << endl;
    cin >> higher;


    else

}

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    cout.precision(10);

    ll points;
    cin >> points;


    return 0;
}
